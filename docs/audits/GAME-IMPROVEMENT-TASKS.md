# GAME-IMPROVEMENT-TASKS — рабочий трекер (claude, сессия с 2026-08-29)

> Источник истины для автономной работы. После compaction — перечитать этот файл
> и `MASTER-ROADMAP.md` (когда появится), продолжить с первой [ ] задачи.
> Легенда: `[ ]` не начато · `[~]` в работе · `[x]` завершено.

## Статус фаз

- [x] Фаза 1 — baseline. Использую существующий `GAME-BASELINE.md` (другая сессия, 232
      строки, сверен и точен) — не дублировал.
- [x] Фаза 2 — реальный playtest. Создано: `PLAYTEST-REPORT.md`, `QA-TEST-CASES.md`,
      `GAMEPLAY-EVIDENCE.md`, аддендум к `BUG-REPORT.md` (верификация BUG-001/009/010 +
      6 новых находок BUG-020..026). Не пройдены руками все 17 от intro до outro — см.
      "Что не успел" в `PLAYTEST-REPORT.md`.
- [x] Фаза 3 — game design / баланс аудит. Созданы `GAME-DESIGN-AUDIT.md` и
      `BALANCE-AUDIT.md` с явным разделением подтверждённых фактов и гипотез.
- [x] Фаза 4 — левел/нарратив/UX/аудио аудит. Созданы `LEVEL-DESIGN-AUDIT.md`,
      `LEVEL-IMPROVEMENT-PLAN.md`, `NARRATIVE-AUDIT.md`, `UX-AUDIT.md`, `AUDIO-AUDIT.md`.
- [x] Фаза 5 — master roadmap. `MASTER-ROADMAP.md` создан, консолидирует все существующие
      аудиты + находки этой сессии в один приоритизированный список (R-1..R-27).
- [~] Фаза 6 — автономные фиксы по приоритету. Исправлены R-1/BUG-001, BUG-003,
      BUG-006, BUG-008, BUG-024, corrupt-save regression, onboarding metric drift и
      keyboard replay/map regressions. Restart из pause перемонтирует миссию, pause-first
      menu и settings subview покрыты e2e;
      архитектурные и инфраструктурные пункты остаются открытыми.
- [~] Фаза 7 — финальная проверка. Выполнены 17-level boot smoke, mobile smoke, targeted
      gameplay checks и unit/e2e regressions; release gate остаётся no-go до ручных и
      инфраструктурных проверок из `RELEASE-READINESS.md`.

## Контекст, важно для продолжения

- Проект: `/Users/aleksandr/Project/other/job/barsik-game`. Автоматические Playwright
  проверки поднимают изолированный Vite server на `http://127.0.0.1:4174`; ранее поднятый
  dev-сервер на другом порту не является доказательством release readiness.
- Рабочее дерево содержит изменения нескольких потоков. Перед merge нужно сверить
  `docs/S1_BOARD.md` и отдельно проверить затронутые `src/`, `supabase/` и `tests/` файлы;
  этот трекер не разрешает автоматически принимать чужие изменения.
- Этой же сессией ранее сегодня уже найдены и **исправлены и подтверждены**:
  camera-в-canopy баги L3 (`Level3Scene.ts`, oldOak), L6 (`Level6Scene.ts`,
  магическое дерево, avoidTreeCanopies), систематический фикс в
  `BaseLevelScene.ts` (`loadTrees()`/`loadWinterDecor()` — camera trailing
  corridor через `clampToPlayArea`), и bug с "утоплением" NPC-друзей в снегу
  на L16 (`Level16Scene.ts:742`, bob-анимация была абсолютным присвоением y).
  Всё запушено в `docs/S1_BOARD.md` под `Done`, не задокументировано отдельно
  в новых QA-файлах этой фазы — нужно перенести в `BUG-REPORT.md` как "found
  and fixed this session", не как открытые баги.
- Инструментарий, уже наработанный и рабочий: `window.__audit()` (dev-only,
  `src/dev/levelAudit.ts`) — структурный аудит уровня (недостижимые
  интерактивы, объекты под землёй, чёрные материалы, hero-off-frame по 4
  углам); собственный occlusion-sweep (camera-в-mesh на сетке точек,
  через `clampToPlayArea`, не `devTeleport` напрямую — см. ниже почему)
  скрипт держу в чате, не сохранён отдельным файлом — при необходимости
  переписать заново, логика описана в `docs/S1_BOARD.md` Done-записи от
  2026-08-29.
- **Важно**: `devTeleport(x,z)` НЕ проходит через `clampToPlayArea` — тестирует
  недостижимые в реальной игре позиции. Любой новый сеттест на границы должен
  сначала клампить через `L.clampToPlayArea(x,z)`, иначе ложные срабатывания.

## Открытые задачи

См. `MASTER-ROADMAP.md` — R-1..R-27, приоритет blocker→critical→high→medium→polish.
Технические части R-2 и R-8 исправлены и покрыты regression/smoke, но release acceptance
ручного полного прохождения и target-device pause/restart semantics остаётся открытым.
R-11 тоже закрыт. Остаётся **R-4** (soft-gate — требует product/legal решения, не
чисто техническая — следующая задача, требующая решения человека, не тех. фикс) и R-9
(возрастная аудитория, тоже человеческое решение); для R-5 остаётся только решение по
незавершённым scene-local stars. Следующая ЧИСТО техническая задача без открытых вопросов —
смотреть `MASTER-ROADMAP.md` раздел High/Medium (R-10, R-13, R-14 и т.д.).

## Закрытые и частичные задачи (эта сессия)

- [x] R-26 / BUG-013 (L0) — стрелка-подсказка на переправе всегда указывала на камень 0.
      Добавлен `furthestStoneIdx`, стрелка целится в следующий непройденный камень; сброс
      при падении в воду (иначе после ре-старта с берега указывала бы далеко вперёд).
      Верифицировано вызовом реальных методов: прогресс 0→1→2 → цель 1→2→3; eject сбрасывает
      корректно. `Level0Scene.ts`.
- [x] R-19 (`levelAudit.ts`) — `black-material` не учитывал emissive-компенсацию, ложно
      флагал уже исправленные L4/L9/L16. Верифицировано живьём на L4 — находка пропала.
- [x] R-4 / BUG-004 (critical) — решение продукта (пользователь): родительский гейт, не полное
      удаление сбора контактов. `SoftGateModal.tsx` — пример на сложение перед формой
      телефона/email, для всех 3 типов гейта. Плюс корневой фикс: `SoftGateController.tsx`
      Gate 2 не проверял `sessionPlayMs` (в отличие от Gate 1) — отсюда «мгновенно для
      вернувшихся». Добавлен `window.__uiStore` dev-hook для QA. Проверено кликами вживую:
      неверный ответ/верный ответ/skip+переоткрытие — всё корректно, без залипания.
- [x] R-18/R-20 (`levelAudit.ts`) — `hero-off-frame` теперь флагает `NaN` вместо молчаливого
      "чисто"; reachability-свип теперь явно говорит, когда не проверил ничего значимого в
      текущей фазе (`phase-scoped-sweep`), а не тихо репортит "0 unreachable" — тот самый
      механизм, из-за которого BUG-001 не был замечен раньше. Не может форсировать чужие фазы
      (архитектурное ограничение инструмента, честно). Верифицировано на L7 (находка появляется)
      и L6 (не появляется, нет ложных срабатываний).
- [x] BUG-027 (не из roadmap, прямая жалоба пользователя) — L12 лёд не скользил и двигался
      очень медленно. Реальная причина: `inertia` применялся как множитель за кадр вместо
      `Math.pow(inertia, dt)` за секунду — на 60fps скорость гасла за 1-2 кадра. Исправлено
      + пересчитаны константы, live-проверено на `learn` и `drop`. `Level12Scene.ts`.
- [x] R-1 / BUG-001 (blocker) — L1 плод #6 недостижим. `Mission1Scene.ts`.
- [x] R-11 / BUG-011 (major) — не тайминг, а логическая ошибка: WASD-подсказка (`showMoveHint`)
      включала `'intro'` в условие показа на 12 из 17 уровней, хотя движение везде заблокировано
      именно в этой фазе (проверил gate индивидуально на каждом — разные имена условий,
      `canMove`/`sliding`/`isActive`/`isFetchPhase`/`isCarryPhase`, но везде исключают `'intro'`).
      L7 было хуже всех: подсказка показывалась ТОЛЬКО в `'intro'`, то есть исчезала именно
      когда управление начинало работать. Убрано `'intro'` из условия в 12 файлах, подтверждено
      детерминированно (не через нестабильный live-таймер этого окружения).
- [~] R-8 / BUG-007 (major) — пауза открывала настройки, а не игровое меню. Теперь: пауза →
      меню (Продолжить/Начать заново/Настройки/Выйти), настройки — вложенный подэкран с
      «Вернуться к паузе». `SettingsPanel.tsx`. Технический путь покрыт e2e; ручное полное
      подтверждение semantics на target devices остаётся release gate.
- [x] R-2 / BUG-002 (critical) — L0 переправа эжектила на середине **любого** прыжка через
      реку: `ejectFromRiver()` не проверяла `this.airborne`. Фикс — `Level0Scene.ts`, один
      guard-clause. Верифицировано дважды живым вводом (`L.keys`+`L.jump()` через реальный
      `L.loop()`, не своя кинематика): до фикса — эжект на 5-м кадре дуги; после — приземление
      на камень 10, 0.38м от центра. Настоящая причина оказалась не в разбеге и не в формуле
      ассерта (обе ранние гипотезы — своя и исходного отчёта — были неверны).
- [x] BUG-003 — replay после завершения сезона: playable pin и CTA повторного прохождения.
- [x] BUG-006 — app-level error boundary и сообщения восстановления для runtime/async init.
- [x] BUG-008 — corrupt local progress больше не поднимает уровень за пределы доступного.
- [x] BUG-005 (частично) — restart из pause menu перемонтирует миссию; mid-level star policy
      остаётся открытой.
- [x] Regression — keyboard-доступность playable map pin; покрыта Playwright.
- [x] Regression — onboarding показывает фактическое число друзей (9), покрыто source check.
- [x] BUG-024 — L4 чёрный металл лебёдки. `Level4Scene.ts`.
- [x] (до начала этой QA-фазы, но той же сессией) — camera-in-canopy L3/L6, общий фикс
  `loadTrees`/`loadWinterDecor`, NPC-в-снегу L16. См. `docs/S1_BOARD.md`.
