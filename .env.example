# Copy to .env (gitignored). Never commit real keys.
OPENAI_API_KEY=sk-proj-...
# From https://platform.tripo3d.ai → API Keys (NOT a task_id like tsk_...)
TRIPO_API_KEY=
# From https://www.remove.bg/dashboard#api-key — used by gen_poses.py
REMOVE_BG_API_KEY=
# From Vercel → Storage → barsik-game-blob — used by scripts/upload-blob.mjs
BLOB_READ_WRITE_TOKEN=
