# Quarantined Barsik look skins

Removed from product 2026-08-27 (shop «Красный pack (тест)» / nude / costumes).

Do not wire back into `heroLooks.ts` or `?look=` without visual QA.
Kept on disk so we can restore a single skin if needed.

Active hero GLB: `../barsik_cool_rigged.glb` (+ `barsik_rigged.glb` fallback).
